# Real Estate Scraper Requirements

# FastAPI
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0

# HTTP clients
httpx>=0.27.0
requests>=2.31.0

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0
parsel>=1.8.0

# Headless browser (pro JS-heavy weby)
playwright>=1.40.0

# Database
asyncpg>=0.29.0
psycopg[binary]>=3.1.0
SQLAlchemy>=2.0.0

# Task scheduling
APScheduler>=3.10.0

# Configuration
PyYAML>=6.0

# Logging
python-json-logger>=2.0.0

# Utilities
python-dotenv>=1.0.0
python-dateutil>=2.8.0
tenacity>=8.2.0            # Retry with exponential backoff (HTTP 429/503 resilience)

# Geo / Route Corridor module (geo/)
geopandas>=0.14.0          # spatial dataframes (reads SHP, GeoJSON, GeoPackage)
shapely>=2.0.0             # geometry operations (buffer, intersect)
pyproj>=3.6.0              # CRS reprojection (WGS84 ↔ S-JTSK / EPSG:5514)
pyogrio>=0.7.0             # fast GDAL-based I/O backend for geopandas (replaces fiona)
gpxpy>=1.6.0               # GPX route file parser

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.1.0
mypy>=1.7.0
