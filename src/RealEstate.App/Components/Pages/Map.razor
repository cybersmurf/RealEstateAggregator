@page "/map"
@using System.Net.Http.Json
@using RealEstate.App.Models
@inject HttpClient Http
@inject IJSRuntime JS
@inject ISnackbar Snackbar
@implements IAsyncDisposable

<PageTitle>Mapa inzerátů – Real Estate Aggregator</PageTitle>

<MudText Typo="Typo.h5" Class="mb-4">
    <MudIcon Icon="@Icons.Material.Filled.Map" Class="mr-2" />
    Mapa inzerátů
</MudText>

<!-- Horní toolbar: filtry + koridor -->
<MudPaper Class="pa-3 mb-3" Elevation="1">
    <MudGrid Spacing="2" AlignItems="Center">

        <!-- Filtry -->
        <MudItem xs="12" sm="6" md="3">
            <MudSelect T="string" @bind-Value="_selectedPropertyType" Label="Typ nemovitosti"
                       Clearable="true" Dense="true" Variant="Variant.Outlined">
                <MudSelectItem T="string" Value="@null">Vše</MudSelectItem>
                <MudSelectItem T="string" Value="@("House")">Dům</MudSelectItem>
                <MudSelectItem T="string" Value="@("Apartment")">Byt</MudSelectItem>
                <MudSelectItem T="string" Value="@("Land")">Pozemek</MudSelectItem>
                <MudSelectItem T="string" Value="@("Cottage")">Chata</MudSelectItem>
                <MudSelectItem T="string" Value="@("Commercial")">Komerční</MudSelectItem>
            </MudSelect>
        </MudItem>

        <MudItem xs="12" sm="6" md="2">
            <MudSelect T="string" @bind-Value="_selectedOfferType" Label="Nabídka"
                       Clearable="true" Dense="true" Variant="Variant.Outlined">
                <MudSelectItem T="string" Value="@null">Vše</MudSelectItem>
                <MudSelectItem T="string" Value="@("Sale")">Prodej</MudSelectItem>
                <MudSelectItem T="string" Value="@("Rent")">Pronájem</MudSelectItem>
                <MudSelectItem T="string" Value="@("Auction")">Dražba</MudSelectItem>
            </MudSelect>
        </MudItem>

        <MudItem xs="12" sm="6" md="2">
            <MudNumericField T="decimal?" @bind-Value="_priceMax" Label="Max. cena (Kč)"
                             Format="N0" HideSpinButtons="true" Dense="true" Variant="Variant.Outlined" />
        </MudItem>

        <MudItem xs="12" sm="6" md="2">
            <MudButton Variant="Variant.Filled" Color="Color.Primary"
                       OnClick="LoadMapPointsAsync" StartIcon="@Icons.Material.Filled.Refresh"
                       Disabled="_isLoading" FullWidth="true">
                @(_isLoading ? "Načítám..." : "Zobrazit")
            </MudButton>
        </MudItem>

        <MudItem xs="12" sm="12" md="3">
            <MudText Typo="Typo.caption" Color="Color.Secondary">
                @(_mapPoints?.Count > 0 ? $"{_mapPoints.Count} inzerátů s GPS" : "Bez bodů na mapě")
            </MudText>
        </MudItem>

    </MudGrid>
</MudPaper>

<!-- Uložené oblasti -->
@if (_savedAreas is { Count: > 0 })
{
    <MudPaper Class="pa-2 mb-3" Elevation="1">
        <MudText Typo="Typo.caption" Color="Color.Secondary" Class="mb-1">
            <MudIcon Icon="@Icons.Material.Filled.Bookmarks" Size="Size.Small" Class="mr-1" />
            Uložené oblasti
        </MudText>
        <div class="d-flex flex-wrap gap-1">
            @foreach (var area in _savedAreas)
            {
                <MudChip T="string" Size="Size.Small" Color="Color.Primary" Variant="Variant.Outlined"
                         OnClick="@(() => LoadSavedAreaAsync(area))"
                         aria-label="@($"Načíst oblast: {area.Name}")"
                         title="@($"{area.StartCity} → {area.EndCity}, buffer {area.BufferMeters / 1000.0:F0} km")">
                    @area.Name
                    @if (area.ListingCount > 0)
                    {
                        <span class="ml-1 mud-text-disabled">(@area.ListingCount)</span>
                    }
                </MudChip>
            }
        </div>
    </MudPaper>
}

<!-- Sekce koridoru -->
<MudExpansionPanels Class="mb-3">
    <MudExpansionPanel Text="🗺️ Vyhledávání v koridoru (buffer kolem trasy)" IsInitiallyExpanded="@_corridorExpanded">
        <MudGrid Spacing="2" AlignItems="Center">
            <MudItem xs="12" sm="5">
                <MudTextField @bind-Value="_corridorStart" Label="Počáteční bod"
                              Placeholder="např. Štítary nebo 48.9,15.9"
                              Variant="Variant.Outlined" Dense="true"
                              AdornmentIcon="@Icons.Material.Filled.TripOrigin" Adornment="Adornment.Start" />
            </MudItem>
            <MudItem xs="12" sm="5">
                <MudTextField @bind-Value="_corridorEnd" Label="Cílový bod"
                              Placeholder="např. Pohořelice nebo 48.98,16.52"
                              Variant="Variant.Outlined" Dense="true"
                              AdornmentIcon="@Icons.Material.Filled.Place" Adornment="Adornment.Start" />
            </MudItem>
            <MudItem xs="12" sm="2">
                <MudNumericField T="int" @bind-Value="_corridorBufferM" Label="Buffer (m)"
                                 Min="500" Max="50000" Step="500" Dense="true" Variant="Variant.Outlined" />
            </MudItem>
            <MudItem xs="12" sm="6">
                <MudCheckBox @bind-Value="_corridorUseRoute" Label="Použít reálnou silniční trasu (OSRM)" Dense="true" />
            </MudItem>
            <MudItem xs="12" sm="6">
                <MudTextField @bind-Value="_corridorSaveName" Label="Uložit oblast jako (volitelné)"
                              Placeholder="Neukládat" Variant="Variant.Outlined" Dense="true" />
            </MudItem>
            <MudItem xs="12" sm="3">
                <MudButton Variant="Variant.Filled" Color="Color.Secondary"
                           OnClick="BuildCorridorAsync" Disabled="_isCorridorLoading" FullWidth="true"
                           StartIcon="@Icons.Material.Filled.Directions">
                    @(_isCorridorLoading ? "Počítám..." : "Hledat v koridoru")
                </MudButton>
            </MudItem>
            <MudItem xs="12" sm="3">
                <MudButton Variant="Variant.Text" Color="Color.Default"
                           OnClick="ClearCorridorAsync" Disabled="_corridorWkt == null" FullWidth="true"
                           StartIcon="@Icons.Material.Filled.Clear">
                    Vymazat koridor
                </MudButton>
            </MudItem>
            @if (_corridorResult != null)
            {
                <MudItem xs="12">
                    <MudAlert Severity="Severity.Info" Dense="true">
                        Koridor: <b>@_corridorStart → @_corridorEnd</b> · buffer @((_corridorResult.BufferMeters / 1000.0).ToString("F1")) km ·
                        <b>@_corridorResult.ListingCount inzerátů</b> uvnitř koridoru
                        @if (_corridorResult.SavedAreaId.HasValue)
                        {
                            <span> · Oblast uložena ✓</span>
                        }
                    </MudAlert>
                </MudItem>
            }

            <!-- ── GPX upload ───────────────────────────────────────────────── -->
            <MudItem xs="12">
                <MudDivider Class="my-1" />
                <MudText Typo="Typo.caption" Color="Color.Secondary" Class="mb-1">
                    nebo nahraj GPX soubor trasy
                </MudText>
            </MudItem>
            <MudItem xs="12" sm="6">
                <MudFileUpload T="IReadOnlyList<IBrowserFile>"
                               FilesChanged="@OnGpxFilesChanged"
                               Accept=".gpx"
                               MaximumFileCount="1"
                               aria-label="Nahrát GPX soubor trasy">
                    <ActivatorContent>
                        <MudButton Variant="Variant.Outlined" Color="Color.Default" FullWidth="true"
                                   StartIcon="@Icons.Material.Filled.Route">
                            @if (_gpxFile != null)
                            {
                                <span>@_gpxFile.Name – změnit</span>
                            }
                            else
                            {
                                <span>Vybrat GPX soubor</span>
                            }
                        </MudButton>
                    </ActivatorContent>
                </MudFileUpload>
            </MudItem>
            <MudItem xs="12" sm="3">
                <MudButton Variant="Variant.Filled" Color="Color.Tertiary"
                           OnClick="BuildCorridorFromGpxAsync"
                           Disabled="@(_gpxFile == null || _isCorridorLoading)"
                           FullWidth="true"
                           StartIcon="@Icons.Material.Filled.Upload">
                    @(_isCorridorLoading ? "Počítám..." : "Hledat v GPX koridoru")
                </MudButton>
            </MudItem>
            @if (_gpxCorridorResult != null)
            {
                <MudItem xs="12">
                    <MudAlert Severity="Severity.Success" Dense="true">
                        GPX koridor: <b>@_gpxFile?.Name</b> · buffer @((_gpxCorridorResult.BufferMeters / 1000.0).ToString("F1")) km ·
                        <b>@_gpxCorridorResult.ListingCount inzerátů</b> uvnitř koridoru
                        @if (_gpxCorridorResult.SavedAreaId.HasValue)
                        {
                            <span> · Oblast uložena ✓</span>
                        }
                    </MudAlert>
                </MudItem>
            }
        </MudGrid>
    </MudExpansionPanel>
</MudExpansionPanels>

<!-- Mapa -->
<MudPaper Elevation="2" Style="position:relative">
    @if (_isLoading || _isCorridorLoading)
    {
        <MudProgressLinear Color="Color.Primary" Indeterminate="true" />
    }
    <div id="@_mapId" style="height: 620px; width: 100%; border-radius: 4px;"></div>
</MudPaper>

<!-- Statistiky koordinátů + geocoding -->
@if (_geocodeStats != null)
{
    <MudPaper Class="pa-3 mt-2" Elevation="1">
        <MudGrid Spacing="1" AlignItems="Center">
            <MudItem xs="12" sm="8">
                <MudText Typo="Typo.body2">
                    <MudIcon Icon="@Icons.Material.Filled.GpsFixed" Size="Size.Small" Class="mr-1" />
                    GPS pokrytí:
                    <strong>@_geocodeStats.WithCoords / @_geocodeStats.Total inzerátů</strong>
                    (@((_geocodeStats.Total > 0 ? (double)_geocodeStats.WithCoords / _geocodeStats.Total * 100 : 0.0).ToString("F0")) %)
                    @if (_geocodeStats.ActiveWithoutCoords > 0)
                    {
                        <MudChip T="string" Color="Color.Warning" Size="Size.Small" Class="ml-2">
                            @_geocodeStats.ActiveWithoutCoords bez GPS
                        </MudChip>
                    }
                    else
                    {
                        <MudChip T="string" Color="Color.Success" Size="Size.Small" Class="ml-2">Vše geokódováno ✓</MudChip>
                    }
                </MudText>
                @if (_lastGeocodeResult != null)
                {
                    <MudText Typo="Typo.caption" Color="Color.Secondary" Class="mt-1">
                        Poslední batch: @_lastGeocodeResult.Succeeded přidáno, @_lastGeocodeResult.Failed selhalo,
                        zbývá @_lastGeocodeResult.RemainingWithoutGps · ~@(_lastGeocodeResult.AvgMsPerRequest) ms/req
                    </MudText>
                }
            </MudItem>
            @if (_geocodeStats.ActiveWithoutCoords > 0)
            {
                <MudItem xs="12" sm="4" Class="d-flex justify-end">
                    <MudButton Variant="Variant.Outlined" Color="Color.Warning"
                               OnClick="RunGeocodingBatchAsync"
                               Disabled="_isGeocoding"
                               StartIcon="@Icons.Material.Filled.LocationSearching"
                               Size="Size.Small">
                        @(_isGeocoding ? "Geocoduji 20 inzerátů..." : $"Geocodovat dalších 20")
                    </MudButton>
                </MudItem>
            }
        </MudGrid>
    </MudPaper>
}

<!-- Statistiky stažených fotek -->
@if (_photoStats != null)
{
    <MudPaper Class="pa-3 mt-2" Elevation="1">
        <MudGrid Spacing="1" AlignItems="Center">
            <MudItem xs="12" sm="8">
                <MudText Typo="Typo.body2">
                    <MudIcon Icon="@Icons.Material.Filled.Photo" Size="Size.Small" Class="mr-1" />
                    Fotky uloženy:
                    <strong>@_photoStats.WithStoredUrl / @_photoStats.Total fotek</strong>
                    (@_photoStats.PercentStored %)
                    @if (_photoStats.WithoutStoredUrl > 0)
                    {
                        <MudChip T="string" Color="Color.Info" Size="Size.Small" Class="ml-2">
                            @_photoStats.WithoutStoredUrl ke stažení
                        </MudChip>
                    }
                    else
                    {
                        <MudChip T="string" Color="Color.Success" Size="Size.Small" Class="ml-2">Vše staženo ✓</MudChip>
                    }
                </MudText>
                @if (_lastPhotoResult != null)
                {
                    <MudText Typo="Typo.caption" Color="Color.Secondary" Class="mt-1">
                        Poslední batch: @_lastPhotoResult.Succeeded staženo, @_lastPhotoResult.Failed selhalo,
                        zbývá @_lastPhotoResult.RemainingWithoutStored · ~@(_lastPhotoResult.AvgMsPerPhoto) ms/fotka
                    </MudText>
                }
            </MudItem>
            @if (_photoStats.WithoutStoredUrl > 0)
            {
                <MudItem xs="12" sm="4" Class="d-flex justify-end">
                    <MudButton Variant="Variant.Outlined" Color="Color.Info"
                               OnClick="RunPhotoDownloadBatchAsync"
                               Disabled="_isDownloadingPhotos"
                               StartIcon="@Icons.Material.Filled.CloudDownload"
                               Size="Size.Small"
                               aria-label="Stáhnout dávku fotek lokálně">
                        @(_isDownloadingPhotos ? "Stahuji 100 fotek..." : "Stáhnout 100 fotek")
                    </MudButton>
                </MudItem>
            }
        </MudGrid>
    </MudPaper>
}

@code {
    private const string _mapId = "realestateMap";
    private DotNetObjectReference<Map>? _dotNetRef;

    // Map data
    private List<ListingMapPointApiDto>? _mapPoints;
    private bool _isLoading;

    // Filtry
    private string? _selectedPropertyType;
    private string? _selectedOfferType;
    private decimal? _priceMax;

    // Koridor
    private string _corridorStart = "";
    private string _corridorEnd = "";
    private int _corridorBufferM = 5000;
    private bool _corridorUseRoute = true;
    private string? _corridorSaveName;
    private bool _corridorExpanded = false;
    private bool _isCorridorLoading;
    private CorridorResultApiDto? _corridorResult;
    private string? _corridorWkt;

    // GPX koridor
    private IBrowserFile? _gpxFile;
    private CorridorResultApiDto? _gpxCorridorResult;

    // Statistiky geocodingu
    private GeocodeStatsDto? _geocodeStats;
    private bool _isGeocoding;
    private BulkGeocodeResultApiDto? _lastGeocodeResult;

    // Statistiky fotek
    private PhotoStatsApiDto? _photoStats;
    private bool _isDownloadingPhotos;
    private PhotoDownloadResultApiDto? _lastPhotoResult;

    // Uložené oblasti
    private List<SavedAreaApiDto>? _savedAreas;

    // ─────────────────────────────────────────────────────────────────────

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            _dotNetRef = DotNetObjectReference.Create(this);
            await JS.InvokeVoidAsync("leafletMap.init", _mapId, 49.0, 16.6, 8);
            await LoadMapPointsAsync();
            await LoadGeocodeStatsAsync();
            await LoadPhotoStatsAsync();
            await LoadSavedAreasAsync();
        }
    }

    private async Task LoadMapPointsAsync()
    {
        _isLoading = true;
        StateHasChanged();

        try
        {
            var url = BuildMapPointsUrl();
            _mapPoints = await Http.GetFromJsonAsync<List<ListingMapPointApiDto>>(url);

            if (_mapPoints != null)
            {
                await JS.InvokeVoidAsync("leafletMap.setMarkers", _mapId, _mapPoints, _dotNetRef);

                if (string.IsNullOrEmpty(_corridorWkt))
                    await JS.InvokeVoidAsync("leafletMap.fitMarkers", _mapId);
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Chyba při načítání bodů: {ex.Message}", Severity.Error);
        }
        finally
        {
            _isLoading = false;
            StateHasChanged();
        }
    }

    private async Task BuildCorridorAsync()
    {
        if (string.IsNullOrWhiteSpace(_corridorStart) || string.IsNullOrWhiteSpace(_corridorEnd))
        {
            Snackbar.Add("Zadejte počáteční i cílový bod koridoru.", Severity.Warning);
            return;
        }

        _isCorridorLoading = true;
        StateHasChanged();

        try
        {
            var request = new
            {
                start = _corridorStart,
                end = _corridorEnd,
                bufferMeters = _corridorBufferM,
                useRoute = _corridorUseRoute,
                saveAsName = string.IsNullOrWhiteSpace(_corridorSaveName) ? null : _corridorSaveName
            };

            var response = await Http.PostAsJsonAsync("api/spatial/corridor", request);
            if (!response.IsSuccessStatusCode)
            {
                var err = await response.Content.ReadAsStringAsync();
                Snackbar.Add($"Chyba koridoru: {err}", Severity.Error);
                return;
            }

            _corridorResult = await response.Content.ReadFromJsonAsync<CorridorResultApiDto>();
            _corridorWkt = _corridorResult?.PolygonWkt;

            if (!string.IsNullOrEmpty(_corridorWkt))
            {
                // Nakresli koridor + vyhledej inzeráty uvnitř
                await JS.InvokeVoidAsync("leafletMap.drawCorridor", _mapId, _corridorWkt);
                await SearchInCorridorAsync(_corridorWkt);
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Chyba: {ex.Message}", Severity.Error);
        }
        finally
        {
            _isCorridorLoading = false;
            StateHasChanged();
        }
    }

    private void OnGpxFilesChanged(IReadOnlyList<IBrowserFile>? files)
    {
        _gpxFile = files?.FirstOrDefault();
        _gpxCorridorResult = null;
    }

    private async Task BuildCorridorFromGpxAsync()
    {
        if (_gpxFile == null)
        {
            Snackbar.Add("Vyberte GPX soubor.", Severity.Warning);
            return;
        }

        _isCorridorLoading = true;
        _gpxCorridorResult = null;
        StateHasChanged();

        try
        {
            using var content = new MultipartFormDataContent();
            var stream = _gpxFile.OpenReadStream(maxAllowedSize: 10 * 1024 * 1024);
            content.Add(new StreamContent(stream), "gpxFile", _gpxFile.Name);

            var saveName = string.IsNullOrWhiteSpace(_corridorSaveName) ? null : _corridorSaveName;
            var url = $"api/spatial/corridor-from-gpx?bufferMeters={_corridorBufferM}" +
                      (saveName != null ? $"&saveName={Uri.EscapeDataString(saveName)}" : "");

            var response = await Http.PostAsync(url, content);
            if (!response.IsSuccessStatusCode)
            {
                var err = await response.Content.ReadAsStringAsync();
                Snackbar.Add($"Chyba GPX koridoru: {err}", Severity.Error);
                return;
            }

            _gpxCorridorResult = await response.Content.ReadFromJsonAsync<CorridorResultApiDto>();
            _corridorWkt = _gpxCorridorResult?.PolygonWkt;

            if (!string.IsNullOrEmpty(_corridorWkt))
            {
                await JS.InvokeVoidAsync("leafletMap.drawCorridor", _mapId, _corridorWkt);
                await SearchInCorridorAsync(_corridorWkt);
                if (saveName != null) await LoadSavedAreasAsync();
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Chyba: {ex.Message}", Severity.Error);
        }
        finally
        {
            _isCorridorLoading = false;
            StateHasChanged();
        }
    }

    private async Task SearchInCorridorAsync(string wkt)
    {
        try
        {
            var request = new
            {
                polygonWkt = wkt,
                propertyType = _selectedPropertyType,
                offerType = _selectedOfferType,
                priceMax = _priceMax,
                page = 1,
                pageSize = 500
            };

            var response = await Http.PostAsJsonAsync("api/spatial/search", request);
            if (response.IsSuccessStatusCode)
            {
                var result = await response.Content.ReadFromJsonAsync<List<ListingMapPointApiDto>>();
                if (result != null)
                {
                    _mapPoints = result;
                    await JS.InvokeVoidAsync("leafletMap.setMarkers", _mapId, _mapPoints, _dotNetRef);
                }
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Chyba při hledání v koridoru: {ex.Message}", Severity.Warning);
        }
    }

    private async Task ClearCorridorAsync()
    {
        _corridorResult = null;
        _corridorWkt = null;
        await JS.InvokeVoidAsync("leafletMap.clearCorridor", _mapId);
        await LoadMapPointsAsync();
    }

    private async Task LoadSavedAreasAsync()
    {
        try
        {
            _savedAreas = await Http.GetFromJsonAsync<List<SavedAreaApiDto>>("api/spatial/areas");
        }
        catch { /* neblokující */ }
        StateHasChanged();
    }

    private async Task LoadSavedAreaAsync(SavedAreaApiDto area)
    {
        if (string.IsNullOrEmpty(area.StartCity) || string.IsNullOrEmpty(area.EndCity))
        {
            Snackbar.Add("Tato oblast nelze obnovit – chybí startovní/cílové město.", Severity.Warning);
            return;
        }

        _corridorStart = area.StartCity;
        _corridorEnd = area.EndCity;
        _corridorBufferM = area.BufferMeters ?? 5000;
        _corridorUseRoute = true;
        _corridorExpanded = true;
        StateHasChanged();

        await BuildCorridorAsync();
    }

    private async Task LoadGeocodeStatsAsync()
    {
        try
        {
            _geocodeStats = await Http.GetFromJsonAsync<GeocodeStatsDto>("api/spatial/geocode-stats");
        }
        catch { /* neblokující */ }
        StateHasChanged();
    }

    private async Task RunGeocodingBatchAsync()
    {
        _isGeocoding = true;
        StateHasChanged();
        try
        {
            var response = await Http.PostAsync("api/spatial/bulk-geocode?batchSize=20", null);
            if (response.IsSuccessStatusCode)
            {
                _lastGeocodeResult = await response.Content.ReadFromJsonAsync<BulkGeocodeResultApiDto>();
                Snackbar.Add($"Geocodováno: {_lastGeocodeResult?.Succeeded} ✓, zbývá {_lastGeocodeResult?.RemainingWithoutGps}", Severity.Success);
                await LoadGeocodeStatsAsync();
                // Obnov markery na mapě
                if (_corridorWkt != null)
                    await SearchInCorridorAsync(_corridorWkt);
                else
                    await LoadMapPointsAsync();
            }
            else
            {
                Snackbar.Add("Geocoding selhal – zkuste znovu", Severity.Error);
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Geocoding chyba: {ex.Message}", Severity.Error);
        }
        finally
        {
            _isGeocoding = false;
            StateHasChanged();
        }
    }
    private async Task LoadPhotoStatsAsync()
    {
        try
        {
            _photoStats = await Http.GetFromJsonAsync<PhotoStatsApiDto>("api/photos/stats");
        }
        catch { /* neblokující */ }
        StateHasChanged();
    }

    private async Task RunPhotoDownloadBatchAsync()
    {
        _isDownloadingPhotos = true;
        StateHasChanged();
        try
        {
            var response = await Http.PostAsync("api/photos/bulk-download?batchSize=100", null);
            if (response.IsSuccessStatusCode)
            {
                _lastPhotoResult = await response.Content.ReadFromJsonAsync<PhotoDownloadResultApiDto>();
                Snackbar.Add($"Staženo: {_lastPhotoResult?.Succeeded} fotek ✓, zbývá {_lastPhotoResult?.RemainingWithoutStored}", Severity.Success);
                await LoadPhotoStatsAsync();
            }
            else
            {
                Snackbar.Add("Stahování fotek selhalo – zkuste znovu", Severity.Error);
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Chyba stahování fotek: {ex.Message}", Severity.Error);
        }
        finally
        {
            _isDownloadingPhotos = false;
            StateHasChanged();
        }
    }
    private string BuildMapPointsUrl()
    {
        var parts = new List<string>();
        if (!string.IsNullOrEmpty(_selectedOfferType)) parts.Add($"offerType={Uri.EscapeDataString(_selectedOfferType)}");
        if (_priceMax.HasValue) parts.Add($"priceMax={_priceMax.Value}");
        var qs = parts.Count > 0 ? "?" + string.Join("&", parts) : "";
        return $"api/spatial/map-points{qs}";
    }

    [JSInvokable]
    public void OnMarkerClicked(Guid listingId)
    {
        // Callback z Leaflet – zatím jen log, rozšiřitelné na panel
        _ = listingId;
    }

    public async ValueTask DisposeAsync()
    {
        try { await JS.InvokeVoidAsync("leafletMap.destroy", _mapId); } catch { }
        _dotNetRef?.Dispose();
    }

    // ─── DTO records (pouze pro Map.razor – nemapujeme přes globální Models) ──

    private sealed record ListingMapPointApiDto(
        Guid Id, string Title, decimal? Price, string LocationText,
        double Latitude, double Longitude,
        string PropertyType, string OfferType,
        string SourceCode, string? MainPhotoUrl);

    private sealed record CorridorResultApiDto(
        string PolygonWkt,
        double StartLat, double StartLon,
        double EndLat, double EndLon,
        int BufferMeters,
        int ListingCount,
        Guid? SavedAreaId);

    private sealed record GeocodeStatsDto(
        int Total, int WithCoords, int ActiveWithoutCoords,
        int FromScraper, int FromNominatim);

    private sealed record BulkGeocodeResultApiDto(
        int Processed, int Succeeded, int Failed,
        int RemainingWithoutGps, int AvgMsPerRequest);

    private sealed record SavedAreaApiDto(
        Guid Id, string Name, string? Description, string AreaType,
        string? StartCity, string? EndCity, int? BufferMeters,
        bool IsActive, DateTime CreatedAt, int ListingCount = 0);

    private sealed record PhotoStatsApiDto(
        int Total, int WithStoredUrl, int WithoutStoredUrl, double PercentStored);

    private sealed record PhotoDownloadResultApiDto(
        int Processed, int Succeeded, int Failed,
        int RemainingWithoutStored, double AvgMsPerPhoto);
}
